<h1>編集画面</h1>
<!--  update_list_pathをlist_pathに変更。資料に指示はない-->
<%= form_with model: @list, url: list_path(@list.id), method: :patch do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>
<% end %>